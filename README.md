# AppFolio - Demo

这是基于两张原型图（应用列表页面与应用详情页面）生成的 Next.js 网站骨架。

技术栈：Next.js + React（JavaScript），可部署到 Vercel。

运行：

```bash
# 安装依赖
npm install
# 本地开发
npm run dev
```

页面：
- `/` - 应用列表（cards 网格）
- `/apps/[slug]` - 应用详情（头部、截图、描述、更新日志、评分、信息）
